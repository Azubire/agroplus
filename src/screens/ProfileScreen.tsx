import { View } from "react-native";
import React from "react";
import Center from "../components/Center";
import { Text } from "react-native-paper";

const Profile = () => {
  return (
    <Center>
      <Text variant="titleLarge">Profile Screen</Text>
    </Center>
  );
};

export default Profile;
